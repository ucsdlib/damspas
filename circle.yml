machine:
  ruby:
    version:
      2.2.3        
  environment:
    CIRCLE_ENV: test
    RAILS_ENV: test

dependencies:
  cache_directories:
    - vagrant
    - ~/.vagrant.d
    - dams-vagrant
  pre:
    - sudo apt-get update;
    - wget http://download.virtualbox.org/virtualbox/4.3.28/virtualbox-4.3_4.3.28-100309~Ubuntu~precise_amd64.deb
    - sudo dpkg -i virtualbox-4.3_4.3.28-100309~Ubuntu~precise_amd64.deb
    - sudo /etc/init.d/vboxdrv setup
    - VBoxManage --version 
    - wget https://releases.hashicorp.com/vagrant/1.8.4/vagrant_1.8.4_x86_64.deb
    - sudo dpkg -i vagrant_1.8.4_x86_64.deb
    - bash ./install-dams-vagrant.sh
  override:
    - bundle check --path=vendor/bundle || bundle install --path=vendor/bundle --jobs=4 --retry=3

database:
  override:
    - bundle exec rake db:migrate

test:
  override:
    - bundle exec rspec ./spec/features/file_spec.rb
  post:
    - vagrant destroy --force