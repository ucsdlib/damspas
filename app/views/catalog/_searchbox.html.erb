<% if query_has_constraints? %>
  <%= form_tag catalog_index_url, :method => :get, :class => 'form-search', :id => 'dc-search-box-mini' do %>
    <% if params['f'] %>
      <% params[:f][:unit_code_tesim] = [unit_code] if !unit_code.nil? && !unit_code.blank?%>
      <input type="hidden" name="xf" value="<%= params['f'] %>"/>
    <% end %>
<% search_text = unit && !unit.blank? ? unit : 'this collection...' %>
<fieldset>
    <label class="hide-text" for="q">Search Digital Collections</label>
    <input type="text" placeholder="Search <%= search_text %>" name="q" id="q" class="" value="<%= params['q'] %>">
  <input type="submit" id="search-button" value="">
</fieldset>

  <% end %>
<% end %>


