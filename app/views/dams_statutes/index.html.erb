<h1>Statutes</h1>

<div class="span6">
	<ul><%= render_statute_list %></ul>
</div>

<div>
	<% if can? :create, DamsStatute%>
	<%= link_to "Create Statute", new_dams_statute_path, :class => 'btn btn-primary' %>
	<% end %>
</div>
</div>
