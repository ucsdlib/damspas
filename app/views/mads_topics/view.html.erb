<h1><%= @mads_topic.name.first %></h1>

<div>
  <dl>
    <dt>TopicElement:</dt>
    <dd><%= @mads_topic.elementList[0].elementValue %></dd>
    <dt>Scheme:</dt>
    <dd><%= @mads_topic.scheme.first.pid %></dd>
    <dt>ExternalAuthority:</dt>
    <dd><%= @mads_topic.externalAuthority.first %></dd>
  </dl>
  <%= render :partial => 'shared/edit_fields/view_form_hidden_fields' , :locals => {:object => @mads_topic, :parent_id => params[:parent_id], :parent_class => params[:parent_class]}%>
</div>
