<%= f.fields_for :scheme do |se| %>
  <%= se.label :id, "Vocabulary" %>
  <%= se.select :id, options_for_select(@mads_schemes), {:include_blank => 'Select Vocabulary'}, :required => "true", :style => "width:400px" %>
<% end %>