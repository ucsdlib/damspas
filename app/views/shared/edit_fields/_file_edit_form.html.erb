<% file =object.file
	i = 0
	if file != nil
		while i < file.size  do
%>
				<%= hidden_field_tag "#{object_type}[file_attributes][#{i}][id]", "#{Rails.configuration.id_namespace}#{object.id}/#{file[i].id}"%>															
<%			
			i +=1		
		end
    end
%> 
