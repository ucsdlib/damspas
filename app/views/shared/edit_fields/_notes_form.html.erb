<h3>Note</h3>
<div id="noteSection">
  <%= f.fields_for :note do |builder| %>
    <%= render "shared/edit_fields/note_fields", :f => builder %>              
  <% end %>
</div>
<p><%= link_to_add_fields "Add Note", f, :note, "DamsNote", "#{object_type}"%></p>