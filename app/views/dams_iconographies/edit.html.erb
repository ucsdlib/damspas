<% @page_title =  "Edit Iconography" %>
<script type="text/javascript">

$(document).ready(function(){

  getMultiAutoCompleteList("Iconography");

});
</script>
<div class="row-fluid">
	<div class="span10 offset1">
	
		<h2>Edit Iconography</h2>
		
		<%= form_for @dams_iconography do |f| %>
		
			<%= render 'error_messages', object: f.object %>
		
			<%= label_tag 'dams_iconography[name][]', "Name" %>
			<%= text_field_tag 'dams_iconography[name][]', @dams_iconography.name[0], class: "typeahead" %>
		
			<%= label_tag 'dams_iconography[externalAuthority][]', "ExternalAuthority" %>
			<%= text_field_tag 'dams_iconography[externalAuthority][]', @dams_iconography.externalAuthority, class: "input-block-level" %>

	 	  <%= f.fields_for :scheme, include_id: false do |se| %>
	        <%= se.label :id, "Scheme" %>
	        <%= se.select :id, options_for_select(@mads_schemes, :selected => @scheme_id) , :prompt=>"Select MadsScheme" %>
	      <% end %>
	      <%= f.fields_for :iconographyElement do |te| %>
	        <%= te.hidden_field :elementValue, class: "eleTypeahead" %>
	      <% end %>
							
			<div class="form-actions">
				<button type="submit" class="btn btn-primary pull-right">Save changes</button>
				<%= link_to "Cancel", dams_iconography_path(@dams_iconography), class: "btn btn-link pull-right" %>
			</div>
				
		
		<%end%>
	
	</div>
</div>
