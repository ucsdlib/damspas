Hello <%= @user.email %>!

Here is your link to sign in to UCSD Digital Library:

<%= URI.join(root_url, new_user_session_path).to_s + "?auth_token=" + @user.authentication_token + "&email=" + @user.email %>
