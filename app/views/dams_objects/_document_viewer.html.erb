<% if defined?(filePath) and defined?(displayFilePath) and can? :update, @document%>

  <% viewFilePath = filePath.gsub('/download', '') %>
  
    <%= link_to image_tag(displayFilePath, :alt => ''), viewFilePath %>

<% end %>
